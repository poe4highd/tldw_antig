# 2026-01-17 历史存档日志 (Backfill)

## 任务：实现用户专属任务历史记录

### 1. 需求背景
- **问题**：仪表盘展示的是系统全局历史，缺乏用户私密性和个性化管理。

### 2. 方案实施
- **后端**：
    - 接口 `/process` 和 `/upload` 增加 `user_id` 记录。
    - 引入 `submissions` 表关联用户与视频任务。
- **前端**：
    - 任务中心 (`/tasks`) 与个人仪表盘 (`/dashboard`) 增加基于 `user_id` 的查询过滤，实现数据隔离。

### 3. 回顾
- **成效**：完善了多用户共用的基础架构，提升了系统作为正式产品的可用性。

---
